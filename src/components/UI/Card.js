import styled from "styled-components";

const Card = styled.div`
  /* padding: 10px; */
  /* margin: 10px; */
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  border-radius: 20px;
`;

export default Card;